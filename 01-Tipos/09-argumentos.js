function suma(a){ //con PARAMETROS a(b,c,d...) indicamos un valor todavia no definido
    return a + 2;
}
let resultado = suma(5); //el valor que le damos a las funciones cuando llas llamamos, son ARGUMENTOS
console.log(resultado);