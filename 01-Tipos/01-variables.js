camelCase //en JS hay 3 maneras de escribir las variables "UpperCamelCase, camelCase, snake_case" 
let nombre = "Hola mundo"; // let se usa para definir variables, una vez definidas, nunca mas vamos a usar let para hacer referencia a esa variable
let nombreCompleto

console.log(nombreCompleto);

let apellido, animal; //aunque puedes definir varias variables en una misma linea, lo ideal es usar una linea para cada una con su respectivo let
apellido = "Palotes"