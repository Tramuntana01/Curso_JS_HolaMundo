// AND, OR, NOT

// AND &&

// console.log(true && true);  // el tipico (+ & + = +) y (- & + = -) de las matematicas basicas

let mayor = false;
let suscrito = true;

console.log("operador and", mayor && suscrito);

//OR va a devolver si es TRUE si alguna de las variables es true, solamente devuelve false si AMBOS son false.
console.log("operador or", mayor || suscrito);

//NOT !
console.log("operador not", !mayor);
let soloCatalogoInfnatil = !mayor;